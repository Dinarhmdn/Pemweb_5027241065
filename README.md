# Customer Service Backend

Minimal Express + TypeScript backend for customer service app.

Quick start

1. cd Backend
2. npm install
3. copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`
4. npm run dev
